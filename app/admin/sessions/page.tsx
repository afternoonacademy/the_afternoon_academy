import Link from "next/link"

import {
  addDeliverySeat, cancelDeliverySession, createDailyDeliverySession,
  createTutorRoomBooking, generatePaidMonth, recordAttendance,
  removeDeliverySeat, restoreDeliverySession, updateDailyDeliverySession,
} from "@/actions/learners"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { supabaseAdmin } from "@/lib/supabase/admin"

const iso = (date: Date) => date.toISOString().slice(0, 10)
const move = (date: string, days: number) => { const value = new Date(`${date}T12:00:00`); value.setDate(value.getDate() + days); return iso(value) }

export default async function SessionsPage({ searchParams }: { searchParams: Promise<{ date?: string; room?: string; table?: string }> }) {
  const query = await searchParams
  const date = /^\d{4}-\d{2}-\d{2}$/.test(query.date || "") ? query.date! : iso(new Date())
  const room = query.room === "tutor" ? "tutor" : "taa1"
  const table = query.table === "2" ? 2 : 1
  const [{ data: sessions }, { data: seats }, { data: learners }, { data: attendance }, { data: bookings }, { data: leads }] = await Promise.all([
    supabaseAdmin.from("delivery_sessions").select("id, table_number, starts_at, duration_minutes, teacher_name, focus, status").eq("service_date", date),
    supabaseAdmin.from("delivery_seats").select("id, delivery_session_id, learner_id, seat_number, status"),
    supabaseAdmin.from("learners").select("id, first_name, year_group").eq("status", "active").order("first_name"),
    supabaseAdmin.from("attendance_records").select("learner_id, delivery_session_id, status").eq("attendance_date", date),
    supabaseAdmin.from("tutor_room_bookings").select("id, teacher_name, starts_at, ends_at").order("starts_at").limit(12),
    supabaseAdmin.from("parent_leads").select("id, parent_name").not("status", "in", '("closed")').order("created_at", { ascending: false }),
  ])
  const session = (sessions || []).find((item) => item.table_number === table)
  const activeSeats = session ? (seats || []).filter((seat) => seat.delivery_session_id === session.id && seat.status === "scheduled") : []
  const learnerById = new Map((learners || []).map((learner) => [learner.id, learner]))
  const attendanceMap = new Map((attendance || []).map((item) => [`${item.delivery_session_id}:${item.learner_id}`, item.status]))
  const link = (next: Record<string, string>) => `/admin/sessions?${new URLSearchParams({ date, room, table: String(table), ...next })}`

  if (room === "tutor") return <div className="space-y-6 pb-10"><div className="flex gap-2"><Link className="rounded-md border px-4 py-2 text-sm" href={link({ room: "taa1" })}>TAA1 room</Link><span className="rounded-md bg-primary px-4 py-2 text-sm text-primary-foreground">Tutor Room</span></div><div><p className="text-sm text-muted-foreground">One-to-one delivery</p><h2 className="text-3xl font-bold">Tutor Room</h2></div><Card><CardHeader><CardTitle>Book a one-to-one appointment</CardTitle></CardHeader><CardContent><form action={createTutorRoomBooking} className="grid gap-3 sm:grid-cols-2"><select className="h-10 rounded-md border bg-background px-3" name="learnerId"><option value="">Enrolled learner</option>{(learners || []).map((learner) => <option key={learner.id} value={learner.id}>{learner.first_name}</option>)}</select><select className="h-10 rounded-md border bg-background px-3" name="parentLeadId"><option value="">Or parent lead</option>{(leads || []).map((lead) => <option key={lead.id} value={lead.id}>{lead.parent_name}</option>)}</select><Input name="teacherName" placeholder="Teacher" required/><Input name="startsAt" type="datetime-local" required/><Input name="endsAt" type="datetime-local" required/><Button className="min-h-11">Book Tutor Room</Button></form></CardContent></Card><Card><CardHeader><CardTitle>Upcoming bookings</CardTitle></CardHeader><CardContent className="space-y-2">{(bookings || []).map((booking) => <p className="rounded border p-3 text-sm" key={booking.id}>{new Date(booking.starts_at).toLocaleString("en-GB")} · {booking.teacher_name}</p>)}</CardContent></Card></div>

  return <div className="space-y-6 pb-10"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm text-muted-foreground">Daily delivery board</p><h2 className="text-3xl font-bold">TAA1</h2></div><form className="flex gap-2" method="get"><input name="room" type="hidden" value="taa1"/><input name="table" type="hidden" value={table}/><Input className="w-40" defaultValue={date} name="date" type="date"/><Button size="sm">View day</Button></form></div><div className="flex gap-2"><span className="rounded-md bg-primary px-4 py-2 text-sm text-primary-foreground">TAA1 room</span><Link className="rounded-md border px-4 py-2 text-sm" href={link({ room: "tutor" })}>Tutor Room</Link></div><div className="flex items-center justify-between rounded-xl border bg-muted/30 p-3"><Link className="px-3 py-2 text-sm" href={link({ date: move(date,-1) })}>← Previous</Link><p className="font-semibold">{new Intl.DateTimeFormat("en-GB",{weekday:"long",day:"numeric",month:"long"}).format(new Date(`${date}T12:00:00`))}</p><Link className="px-3 py-2 text-sm" href={link({ date: move(date,1) })}>Next →</Link></div><div className="flex gap-2"><Link className={table===1?"rounded-full bg-primary px-4 py-2 text-sm text-primary-foreground":"rounded-full border px-4 py-2 text-sm"} href={link({table:"1"})}>Table 1</Link><Link className={table===2?"rounded-full bg-primary px-4 py-2 text-sm text-primary-foreground":"rounded-full border px-4 py-2 text-sm"} href={link({table:"2"})}>Table 2</Link></div><Card><CardHeader><CardTitle>Table {table} {session ? <span className="text-base font-normal text-muted-foreground">· {session.starts_at.slice(0,5)} · {session.teacher_name} · {session.focus}</span> : null}</CardTitle></CardHeader><CardContent>{!session ? <form action={createDailyDeliverySession} className="grid gap-3 sm:grid-cols-2"><input name="serviceDate" type="hidden" value={date}/><input name="tableNumber" type="hidden" value={table}/><Input name="teacherName" placeholder="Teacher" required/><Input name="focus" defaultValue="General homework support" required/><Input name="startsAt" defaultValue="17:00" type="time" required/><Input name="durationMinutes" defaultValue="50" type="number" required/><Button className="min-h-11 sm:col-span-2">Set up this table</Button></form> : <><details className="mb-4 rounded-lg border p-3"><summary className="cursor-pointer font-medium">Edit this date / manage plan</summary><form action={updateDailyDeliverySession} className="mt-3 grid gap-2 sm:grid-cols-2"><input name="deliverySessionId" type="hidden" value={session.id}/><input name="serviceDate" type="hidden" value={date}/><input name="tableNumber" type="hidden" value={table}/><Input defaultValue={session.teacher_name||""} name="teacherName" required/><Input defaultValue={session.focus||""} name="focus" required/><Input defaultValue={session.starts_at.slice(0,5)} name="startsAt" type="time" required/><Input defaultValue={session.duration_minutes} name="durationMinutes" type="number" required/><Button>Save changes</Button></form><form action={generatePaidMonth} className="mt-3 flex gap-2"><Input defaultValue={date.slice(0,7)} name="month" type="month"/><Button variant="outline">Generate paid month</Button></form></details>{session.status==="cancelled"?<form action={restoreDeliverySession}><input name="deliverySessionId" type="hidden" value={session.id}/><Button>Restore table</Button></form>:<><div className="mx-auto grid max-w-md grid-cols-3 gap-3 rounded-[2rem] border-8 border-amber-900 bg-amber-100 p-6">{[1,2,3,4,5,6].map((number)=>{const seat=activeSeats.find((item)=>item.seat_number===number);const learner=seat?learnerById.get(seat.learner_id):undefined;return <div className={learner?"min-h-24 rounded-full bg-primary p-3 text-center text-xs text-primary-foreground":"min-h-24 rounded-full border-2 border-emerald-500 bg-emerald-50 p-3 text-center text-xs text-emerald-800"} key={number}>{learner?<><p className="font-semibold">{learner.first_name}</p><form action={recordAttendance} className="mt-2"><input name="learnerId" type="hidden" value={learner.id}/><input name="attendanceDate" type="hidden" value={date}/><input name="deliverySessionId" type="hidden" value={session.id}/><input name="status" type="hidden" value="present"/><Button className="h-8 text-xs" disabled={attendanceMap.get(`${session.id}:${learner.id}`)==="present"} size="sm">{attendanceMap.get(`${session.id}:${learner.id}`)==="present"?"✓":"Present"}</Button></form><form action={removeDeliverySeat} className="mt-1"><input name="deliverySeatId" type="hidden" value={seat.id}/><Button className="h-7 text-xs" size="sm" variant="outline">Remove</Button></form></>:<>Seat {number}<p className="mt-2 font-semibold">Available</p></>}</div>})}</div><form action={addDeliverySeat} className="mt-5 grid gap-2 sm:grid-cols-3"><input name="deliverySessionId" type="hidden" value={session.id}/><select className="h-10 rounded-md border bg-background px-3" name="learnerId" required><option value="">Add learner</option>{(learners||[]).map((learner)=><option key={learner.id} value={learner.id}>{learner.first_name}</option>)}</select><select className="h-10 rounded-md border bg-background px-3" name="seatNumber" required><option value="">Seat</option>{[1,2,3,4,5,6].map((n)=><option key={n} value={n}>{n}</option>)}</select><Button>Add for this date</Button></form><form action={cancelDeliverySession} className="mt-4"><input name="deliverySessionId" type="hidden" value={session.id}/><Button variant="outline">Cancel this table</Button></form></>}</>}</CardContent></Card></div>
}